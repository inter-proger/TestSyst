<%= content_for :menu  do%>
<ul>
      <li><%=link_to 'Главная',:root%></li>
      <li>|&nbsp;Вторая</li>
      <li>|&nbsp;Третья</li>
      <li><%= render :partial =>"users/user_bar" %></li>
    </ul>
<% end %>
<h1><%= @theme.title %></h1>
Themes#show
<br/>
Find me at themes/show.html.erb
<% @qtypes=Qtype.all %>
<% @c=6 %>
<table>
<% @theme.questions.each do |t| %>
  <tr>
    <td>
  <%= link_to t.content, discipline_theme_question_path(@discipline,@theme,t)%>
      </td>
      <td>
  <%=  @qtypes[t.qtype_id-@c].title %>
      </td>
      <td>
        <%= link_to image_tag('del.png',:class=>'del'),discipline_theme_question_url(@discipline,@theme,t),:confirm =>'Вы уверены', :method=>:delete  %>
      </td>
  </tr>
<% end %>
  </table>

<%= link_to 'Создать вопрос', new_discipline_theme_question_path(@discipline,@theme) %>

<%= link_to 'Назад', discipline_path(@discipline) %>

