<%= content_for :menu  do%>
<ul>
      <li><%=link_to 'Главная',:root%></li>
      <li>|&nbsp;Вторая</li>
      <li>|&nbsp;Третья</li>
      <li><%= render :partial =>"users/user_bar" %></li>
    </ul>
<% end %>
<h1>Listing configuration_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @configuration_types.each do |configuration_type| %>
  <tr>
    <td><%= configuration_type.Name %></td>
    <td><%= link_to 'Show', configuration_type %></td>
    <td><%= link_to 'Edit', edit_configuration_type_path(configuration_type) %></td>
    <td><%= link_to image_tag('del.png',:class=>'del'), configuration_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Configuration type', new_configuration_type_path %>
